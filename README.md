# Knowledge
Local information service which will function as a proxy between some other API I use

## Dokku
I run this service using `dokku`.

```
git remote add server dokku@server.lan:knowledge
```

API is available via `knowledge.server.lan`
